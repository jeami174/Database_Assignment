<UserControl x:Class="Presentation_WPF_MainApp.Views.ProjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation_WPF_MainApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!--<ListView ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}">-->
        <ListView ItemsSource="{Binding Projects}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Titel" DisplayMemberBinding="{Binding Title}" />
                    <GridViewColumn Header="Startdatum" DisplayMemberBinding="{Binding StartDate}" />
                    <GridViewColumn Header="Slutdatum" DisplayMemberBinding="{Binding EndDate}" />
                    <GridViewColumn Header="Kund" DisplayMemberBinding="{Binding Customer.CustomerName}" />
                    <GridViewColumn Header="Tjänst" DisplayMemberBinding="{Binding Service.ServiceName}" />
                    <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status.StatusTypeName}" />
                    <GridViewColumn Header="Projektledare" DisplayMemberBinding="{Binding User.FirstName}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>

